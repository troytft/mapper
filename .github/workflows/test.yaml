on:
    pull_request:
        branches:
            - master

jobs:
    test:
        runs-on: ubuntu-latest
        container:
            image: prooph/composer:7.2
            volumes:
                - .:/
        steps:
            - name: Install composer
              run: composer install -n
